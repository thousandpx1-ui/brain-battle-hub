Nothing needs to be added to .gitignore since the only file change is adding a single HTML file (`block-blast-game.html`), which is a source/config file and not a build artifact, dependency, or temporary file.