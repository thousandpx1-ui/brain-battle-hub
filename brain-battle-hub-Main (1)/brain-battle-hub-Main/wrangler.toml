# Cloudflare Pages Configuration
[build]
command = "cd artifacts/brain-battle-hub && pnpm install && pnpm run build"
pages_build_output_dir = "artifacts/brain-battle-hub/dist"

[vars]
NODE_VERSION = "24"